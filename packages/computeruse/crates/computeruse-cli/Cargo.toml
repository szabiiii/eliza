[package]
name = "computeruse-cli"
version.workspace = true
edition.workspace = true
description = "CLI for AI-native desktop automation - control any app with MCP"
authors = ["elizaOS Contributors"]
repository = "https://github.com/mediar-ai/computeruse"
homepage = "https://mediar.ai"
documentation = "https://github.com/mediar-ai/computeruse"
readme = "../../README.md"
keywords = ["automation", "gui", "desktop", "mcp", "cli"]
categories = ["command-line-utilities", "development-tools"]
license-file.workspace = true

[[bin]]
name = "computeruse"
path = "src/main.rs"

[[bin]]
name = "cargo-computeruse"
path = "src/bin/cargo-computeruse.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.140"
clap = { version = "4.4", features = ["derive", "env"] }
tokio = { version = "1", features = [
    "rt",
    "macros",
    "rt-multi-thread",
    "process",
    "full",
] }
anyhow = "1.0"

rmcp = { version = "0.9", features = [
    "transport-child-process",
    "client",
    "transport-streamable-http-client-reqwest",
] }

tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "time", "local-time"] }
tracing-appender = "0.2"


dotenvy = "0.15.7"
reqwest = { version = "0.12.22", features = ["json", "rustls-tls"] }

anthropic-sdk = "0.1.5"

# Cron scheduling support
tokio-cron-scheduler = "0.14"
chrono = { version = "0.4", features = ["serde"] }

# Display formatting
colored = "3.0.0"

# System directories
dirs = "5.0"
# Regex for stripping ANSI codes
regex = "1.10"

# For snippet generation
computeruse-mcp-agent = { path = "../computeruse-mcp-agent" }

# Simple HTTP server for receiving telemetry
bytes = "1.11"

# Protobuf support for parsing OTLP data
